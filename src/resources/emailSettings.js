import createResourceDescription from './util/createResourceDescription'

export const emailSettings = createResourceDescription('email_settings', {
  enable_pagination: false,
  enable_id_filter: false,
})
